Placeholder folder. The prototype used text placeholders like "[ illustration: mẹ bế con ]"
instead of real images. Flutter mirrors that pattern with `IllustrationPlaceholder`
(see lib/core/widgets/illustration_placeholder.dart) — a tinted rounded container with a
small label — instead of sourcing real images. Drop real photos/exported PNGs here later.
