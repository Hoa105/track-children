Placeholder folder for custom icon assets. The app currently uses Material Icons
(`Icons.*`) everywhere via `uses-material-design: true`; drop custom SVG/PNG icon sets
here if the prototype's exact iconography needs to be reproduced later.
